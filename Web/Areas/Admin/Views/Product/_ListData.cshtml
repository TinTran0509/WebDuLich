@{
    var lstAction = (List<int>)HttpContext.Current.Session["Action"];
    int id = 0, bannerID = 0;
}
@using Web.Core
@using Web.Model.CustomModel
@model List<ProductModel>
    <table class="table">
        <thead>
            <tr>
                <th>Hình ảnh</th>
                <th>Mã</th>
                <th>Tiêu đề</th>
                <th>Mô tả</th>
                <th>Nội dung</th>
                <th>Ngôn ngữ</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                string group = "group-banner";
                if(bannerID != item.ProductID)
                {
                    group = "";
                }
                <tr class="@group">
                    @if (id != item.ProductID)
                    {
                        <td style="vertical-align: middle;" rowspan="2"><img src="@item.Image" style="width: 50px;" /></td>
                    }
                    @if (id != item.ProductID)
                    {
                        <td style="vertical-align: middle;" rowspan="2">@item.ProductCode</td>
                    }
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@HelperString.TruncateByWord(HelperString.StripHTML(HttpUtility.HtmlDecode(item.Contents)), 110)</td>
                    <td>@item.LangName</td>

                    @if (id != item.ProductID)
                    {
                        <td rowspan="2">
                            <div class="btn-list pull-right mr5">
                                @if (lstAction != null && lstAction.Contains((int)EnumHelper.Action.Edit))
                                {
                                    <a href="/admin/product/edit?id=@item.ProductID" class="btn btn-primary btn-bordered" title="Sửa">
                                        <i class="glyphicon glyphicon-pencil"></i>
                                    </a>
                                }
                                @if (lstAction != null && lstAction.Contains((int)EnumHelper.Action.Delete))
                                {
                                    <a href="#" class="btn btn-danger btn-bordered" onclick="Product.ondelete(@item.ID) " title="Xóa">
                                        <i class="glyphicon glyphicon-trash"></i>
                                    </a>
                                }
                            </div>
                        </td>
                    }
                </tr>
                id = item.ID;
                bannerID = item.ProductID;
            }
        </tbody>
    </table>
    <script>
        $(document).ready(function () {
            $('.width50').on("keypress", function () {
                return event.charCode >= 48 && event.charCode <= 57
            })
            InitClickCheckAllInTable("chkall", function (res) {
                $("#hdfID").val(res);
            });
        });
    </script>
